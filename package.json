{
  "name": "nexus",
  "version": "1.0.0",
  "private": true,
  "description": "NEXUS — Ibrahim's personal operating system (private).",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "dev": "node scripts/build.js && npx serve dist",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "supabase": "^2.111.0"
  }
}
